# Carregado automaticamente pelo Vite em `npm run dev` / `dev:all`.
# Aponta o front para o backend de cotações (server/cotacoes-proxy.mjs),
# encaminhado pelo proxy /api do Vite. Sem segredos aqui.
VITE_COTACOES_ENDPOINT=/api/cotacoes
